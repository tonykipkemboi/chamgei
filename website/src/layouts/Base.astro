---
import '../styles/global.css';
import Analytics from '@vercel/analytics/astro';

export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  faqSchema?: { q: string; a: string }[];
}

const {
  title = 'rekody — Voice Dictation for Everyone',
  description = 'Open-source macOS voice dictation. Hold ⌥Space, speak, release. Text injected at your cursor in any app. Push-to-talk, 100+ languages, powered by Deepgram Nova-3 (~200ms).',
  ogImage = '/og-image.png',
  faqSchema,
} = Astro.props;

const canonicalUrl = new URL(Astro.url.pathname, Astro.site ?? 'https://rekody.com');
const ogImageUrl = new URL(ogImage, Astro.site ?? 'https://rekody.com');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="canonical" href={canonicalUrl} />

    <!-- Primary SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Tony Kipkemboi" />
    <meta name="theme-color" content="#FBFAF4" />

    <!-- Robots -->
    <meta name="robots" content="index, follow, max-image-preview:large" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="rekody" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="rekody — Voice dictation for everyone. Speak. Release. Done." />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@tonykipkemboi" />
    <meta name="twitter:creator" content="@tonykipkemboi" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    <meta name="twitter:image:alt" content="rekody — Voice dictation for everyone. Speak. Release. Done." />

    <!-- Favicons -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico?v=2" />
    <link rel="icon" type="image/png" href="/icon.png?v=2" sizes="1024x1024" />
    <link rel="apple-touch-icon" sizes="180x180" href="/icon.png?v=2" />

    <!-- JSON-LD: SoftwareApplication -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "rekody",
      "applicationCategory": "UtilitiesApplication",
      "operatingSystem": "macOS",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
      "url": "https://rekody.com",
      "description": description,
      "license": "https://opensource.org/licenses/MIT",
      "author": {
        "@type": "Person",
        "name": "Tony Kipkemboi",
        "url": "https://github.com/tonykipkemboi",
      },
    })} />

    <!-- JSON-LD: FAQPage -->
    {faqSchema && faqSchema.length > 0 && (
      <script type="application/ld+json" set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": faqSchema.map(({ q, a }) => ({
          "@type": "Question",
          "name": q,
          "acceptedAnswer": { "@type": "Answer", "text": a },
        })),
      })} />
    )}
  </head>
  <body>
    <slot />
    <Analytics />
  </body>
</html>
